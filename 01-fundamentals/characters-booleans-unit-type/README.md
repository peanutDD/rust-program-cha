## Rust 字符、布尔与单元类型全面教程

本项目对应 [Rust 语言圣经 - 字符、布尔与单元类型](https://course.rs/basic/base-type/char-bool.html)，通过一个完整的大型示例文件，系统讲解：

- `char` 字符类型与 Unicode  
- `bool` 布尔类型与逻辑运算  
- `()` 单元类型及其在函数返回值、控制流中的意义  

### 📁 项目结构

```text
characters-booleans-unit-type/
├── src/
│   └── char_bool_unit_guide.rs   # 主入口 & 全量示例（含 main）
├── Cargo.toml                    # 项目配置
└── rustfmt.toml                  # 代码格式配置
```

### 🚀 快速开始

```bash
cd 01-fundamentals/characters-booleans-unit-type
cargo run
```

`cargo run` 会执行 `char_bool_unit_guide.rs` 中的 `main` 函数，依次打印各章节示例输出。

### 📚 主要学习内容（对应 `char_bool_unit_guide.rs` 中的章节）

- **字符类型 `char`**
  - `char` 的大小与内存表示（始终为 4 字节、Unicode 标量值）
  - 字符字面量与转义序列（`\n`、`\t`、`\\`、`\u{XXXX}` 等）
  - 多语言字符与 Emoji 示例
  - 常用方法：`is_alphabetic` / `is_numeric` / `is_whitespace` / `to_uppercase` 等

- **布尔类型 `bool`**
  - `true` / `false` 与比较表达式
  - 逻辑运算：`&&`、`||`、`!` 及短路求值
  - 在 `if` / `while` / `match` 控制流中的使用

- **单元类型 `()`**
  - 无返回值函数的真实类型
  - 表达式末尾分号导致返回 `()`
  - 在接口设计、占位类型中的应用

- **进阶与实战**
  - Unicode / UTF-8 编码与 `String` / `&str` 的关系
  - 使用 `char` 进行文本处理与分类
  - 结合 `Option` / `Result` 的布尔逻辑与错误处理

### ✅ 教程风格说明

- 全部注释与输出为 **中文**，技术关键字保持英文（如 `char` / `bool` / `Option` 等）  
- 示例代码遵循：
  - 默认使用不可变绑定 `let`，有需要时再使用 `let mut`
  - 用 `Result` / `Option` 表达错误与空值，而不是 `panic!`  
  - 严格区分 **语句与表达式**，在结尾章节有专门对比

### 🔗 推荐阅读

- [Rust 语言圣经 - 字符、布尔与单元类型](https://course.rs/basic/base-type/char-bool.html)  
- [Rust 程序设计语言 - 数据类型](https://kaisery.github.io/trpl-zh-cn/ch03-02-data-types.html)  
- [Rust Reference - Primitive types](https://doc.rust-lang.org/reference/types.html)


