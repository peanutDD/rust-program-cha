## Rust 数值类型全面教程

本项目基于 [Rust 语言圣经 - 数值类型](https://course.rs/basic/base-type/numbers.html) 的内容，对 **整数、浮点数及相关标准库 API** 进行系统性讲解与演示，配合大量可运行示例，帮助你彻底吃透 Rust 的数值体系。

### 📁 项目结构

```text
basic-type/
├── src/
│   └── numbers.rs         # 数值类型完整示例与讲解（单文件入口）
├── Cargo.toml             # 项目配置
└── rustfmt.toml           # 代码格式配置
```

### 🎯 学习目标

- 理解 Rust 中 **整数类型（有符号/无符号/isize/usize）** 的范围与使用场景  
- 掌握 **浮点数 f32/f64、科学计数法** 及相关常量  
- 熟悉 **算术运算、复合赋值运算** 的语义与注意事项  
- 理解 **显式类型转换（`as`）与安全转换（`TryInto`）** 的差异与最佳实践  
- 掌握 **整数溢出处理 API**：`checked_*` / `saturating_*` / `wrapping_*` / `overflowing_*`  
- 理解 **NaN / 无穷大 / 除零** 等浮点特殊值及其陷阱  
- 了解数值类型在 **性能与内存占用** 上的权衡

### 🚀 快速开始

```bash
# 进入项目目录
cd 01-fundamentals/basic-type

# 运行完整数值类型演示
cargo run
```

> 当前 crate 只包含一个入口文件 `src/numbers.rs`，`cargo run` 即会执行其中的所有章节示例。

### 🧩 示例章节概览（对应 `numbers.rs` 内函数）

- `integer_types_basics`：整数类型总览与默认推导规则  
- `integer_literals`：十进制 / 十六进制 / 八进制 / 二进制 / 字节字面量 与类型后缀  
- `floating_point_types`：`f32` / `f64`、科学计数法、精度与最大值  
- `numeric_operations`：整数/浮点四则运算、取余、复合赋值与“没有 ++/--” 的设计  
- `type_conversion`：`as` 转换的截断风险与 `TryInto` 的安全用法  
- `integer_overflow_handling`：四种溢出策略及适用场景  
- `floating_point_special_values`：`NaN` / `INFINITY` / `NEG_INFINITY` / 除零行为  
- 其余章节覆盖：数值方法、精度问题、类型范围、性能考虑与实际应用案例

### ✅ 风格与实践建议

- 默认使用 **不可变绑定 `let`**，只在确有需要时使用 `let mut`  
- 显式展示 **类型推导结果**（如 `i32` / `f64`），便于读者形成直觉  
- 演示数值运算时，优先使用 **安全 API（`checked_*` / `TryInto`）**，再对比不安全写法  
- 在实际业务中：
  - 选择**最小但足够**的整数类型，避免过度使用 `i64` / `u64`  
  - 对外部数据（网络/文件/用户输入）进行 **边界检查与转换错误处理**  

### 🔗 推荐阅读

- [Rust 语言圣经 - 数值类型](https://course.rs/basic/base-type/numbers.html)  
- [Rust 程序设计语言 - 数据类型](https://kaisery.github.io/trpl-zh-cn/ch03-02-data-types.html)  
- [Rust Reference - Numeric types](https://doc.rust-lang.org/reference/types/numeric.html)


