# Rust 字符串与切片全面学习指南

> 基于 https://course.rs/basic/compound-type/string-slice.html 的深度分析

## 📚 项目概述

本项目全面深入地分析 Rust 字符串和切片系统，从基础概念到高级应用，帮助开发者掌握 Rust 字符串处理的核心机制。

## 🎯 学习目标

通过本教程，你将学会：

- ✅ 理解 String 和 &str 的区别
- ✅ 掌握字符串的创建和操作
- ✅ 理解字符串的所有权和借用
- ✅ 掌握切片的概念和使用
- ✅ 学会字符串的格式化
- ✅ 理解字符串的性能特性

## 📖 核心知识点

### 1. 字符串类型

**String vs &str：**
- `String` - 可变的、拥有所有权的字符串
- `&str` - 不可变的字符串切片引用

**基本用法：**
```rust
let s1 = String::from("hello");
let s2 = "world";  // &str
let s3 = s2.to_string();  // String
```

### 2. 字符串操作

**创建和修改：**
```rust
let mut s = String::new();
s.push_str("hello");
s.push(' ');
s.push_str("world");
```

**字符串连接：**
```rust
let s1 = String::from("Hello");
let s2 = String::from("World");
let s3 = s1 + &s2;  // s1 被移动
let s4 = format!("{} {}", s2, "Rust");
```

### 3. 字符串索引

**Rust 字符串不支持直接索引：**
```rust
// ❌ 不支持
// let c = s[0];

// ✅ 使用切片
let slice = &s[0..4];
```

**字符迭代：**
```rust
for c in s.chars() {
    println!("{}", c);
}

for b in s.bytes() {
    println!("{}", b);
}
```

### 4. 切片 (Slice)

**基本概念：**
```rust
let s = String::from("hello world");
let hello = &s[0..5];
let world = &s[6..11];
```

**字符串切片：**
```rust
let s = "hello world";
let slice: &str = &s[0..5];
```

### 5. 字符串方法

**常用方法：**
```rust
s.len();                    // 字节长度
s.chars().count();          // 字符数量
s.is_empty();               // 是否为空
s.contains("sub");          // 包含子串
s.replace("old", "new");    // 替换
```

## 🚀 快速开始

```bash
# 运行完整教程
cargo run

# 运行练习题
cargo run exercises

# 运行挑战练习
cargo run challenge
```

## 📖 学习路径

### 1. 基础阶段
- 理解 String 和 &str
- 掌握基本操作
- 学习字符串创建

### 2. 进阶阶段
- 掌握切片操作
- 理解所有权和借用
- 学习字符串方法

### 3. 高级阶段
- 理解 UTF-8 编码
- 优化字符串性能
- 在实际项目中应用

## 💡 最佳实践

1. **优先使用 &str**：作为函数参数
2. **使用 String**：需要拥有所有权时
3. **注意 UTF-8**：字符串是 UTF-8 编码
4. **使用切片**：避免不必要的克隆
5. **性能考虑**：理解字符串操作的开销

## 🔍 常见陷阱

1. **索引问题**：字符串不支持直接索引
2. **所有权混淆**：String vs &str 的所有权
3. **UTF-8 问题**：多字节字符的处理
4. **切片越界**：切片索引可能越界

## 📚 相关资源

- [Rust Book - Strings](https://doc.rust-lang.org/book/ch08-02-strings.html)
- [String Documentation](https://doc.rust-lang.org/std/string/struct.String.html)
- [str Documentation](https://doc.rust-lang.org/std/primitive.str.html)

## 🎯 总结

字符串和切片是 Rust 编程的基础，理解它们对于编写 Rust 程序至关重要。

---

**Happy String Processing! 🦀**
